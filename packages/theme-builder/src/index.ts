type ThemeBuilderOptions = Record<any, any>;

export function defaultTheme(options: ThemeBuilderOptions): ThemeBuilderOptions {
  return options;
}

export function themeBuilder(options: ThemeBuilderOptions): ThemeBuilderOptions {
  return options;
}
